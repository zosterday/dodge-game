using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnManager : MonoBehaviour
{
    //TODO:
    //Spawn on the edge of playarea anywhere around it 
    //angle them to middle of play area
    //rotate around z axis to allow variation, maybe like 42.5 degrees in either direction
    //


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
